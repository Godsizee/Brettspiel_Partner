# Vorlage für .env — Werte eintragen und als .env speichern (wird nicht committet)
PB_HOST=https://pocketbase-boardgame.dasdann.jetzt
PB_EMAIL=superuser@example.com
PB_PASS=geheim

# Nur nötig, solange die PB-Instanz ein selbstsigniertes TLS-Zertifikat nutzt:
# NODE_TLS_REJECT_UNAUTHORIZED=0
